#ifndef MATH_H
#define MATH_H

#include <vector>

namespace math
{
    /**
     * Sum the double vector and return the result
     * @param v The vector to sum
     * @return The sum of the vector
     */
    double sum(const std::vector<double> &v);
} // namespace math
#endif // MATH_H